defaults:
    - training: default
    - data: default
    - _self_

training:
    max_epochs: 1

data:
    batch_size: 64

neptune:
    name: Encoding
    project: 'JPL/rna-sequencing'

accelerator: mps
environment: .env
